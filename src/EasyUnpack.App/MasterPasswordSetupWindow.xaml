<Window x:Class="EasyUnpack.App.MasterPasswordSetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="设置主密码" Width="460" Height="300" ResizeMode="NoResize"
        FontFamily="Segoe UI Variable Text, Microsoft YaHei UI"
        Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
        Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="设置主密码" Style="{StaticResource PageTitleTextStyle}" FontSize="20" />
        <TextBlock Grid.Row="1" Text="主密码用于加密本机密码库" Style="{StaticResource SecondaryTextStyle}" Margin="0,3,0,16" />
        <TextBlock Grid.Row="2" Text="主密码" Style="{StaticResource DialogLabelStyle}" />
        <PasswordBox x:Name="PasswordInput" Grid.Row="3" MinHeight="36" VerticalContentAlignment="Center" />
        <TextBlock Grid.Row="4" Text="确认主密码" Style="{StaticResource DialogLabelStyle}" Margin="0,12,0,6" />
        <PasswordBox x:Name="ConfirmationInput" Grid.Row="5" MinHeight="36" VerticalContentAlignment="Center" />
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button Content="取消" Style="{StaticResource ActionButtonStyle}" IsCancel="True" />
            <Button Content="保存" Style="{StaticResource ActionButtonStyle}" Margin="8,0,0,0" IsDefault="True" Click="Save_Click" />
        </StackPanel>
    </Grid>
</Window>
