<Window x:Class="EasyUnpack.App.PasswordPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="需要密码" Width="460" Height="300" ResizeMode="NoResize"
        FontFamily="Segoe UI Variable Text, Microsoft YaHei UI"
        Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
        Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
        WindowStartupLocation="CenterOwner" Loaded="Window_Loaded">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="输入压缩包密码" Style="{StaticResource PageTitleTextStyle}" FontSize="20" />
        <TextBlock x:Name="ArchiveNameText" Grid.Row="1" Margin="0,4,0,16" Style="{StaticResource SecondaryTextStyle}"
                   TextWrapping="NoWrap" TextTrimming="CharacterEllipsis"
                   ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}" />
        <TextBlock Grid.Row="2" Text="密码" Style="{StaticResource DialogLabelStyle}" />
        <PasswordBox x:Name="PasswordInput" Grid.Row="3" MinHeight="36" VerticalContentAlignment="Center" />
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button Content="跳过" Style="{StaticResource ActionButtonStyle}" IsCancel="True" />
            <Button x:Name="ContinueButton" Content="继续" Style="{StaticResource ActionButtonStyle}"
                    Margin="8,0,0,0" IsDefault="True" Click="Continue_Click" />
        </StackPanel>
    </Grid>
</Window>
