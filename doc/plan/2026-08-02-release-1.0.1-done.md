# Release EasyUnpack 1.0.1

## Scope

- Release embedded ZIP payload detection and extraction normalization as version 1.0.1.
- Keep unrelated user-owned worktree changes out of the release commit.

## Implementation

- Remove confirmed test/build temporary outputs and stale release artifacts.
- Update managed application, core library, and installer metadata to 1.0.1.
- Run the full managed test and Release build gates.
- Build a fresh self-contained installer and verify its version and checksum.
- Install the generated package silently and verify the installed application version.
- Commit the intended source, tests, architecture decision, and completed plans.
- Push `main`, create and push tag `v1.0.1`, publish a GitHub Release with the installer, and verify the public release asset.

## Safety

- Do not remove or modify the supplied MP4 or unrelated untracked files.
- Do not expose GitHub credentials in command output, logs, release notes, or repository files.
- Mark this plan complete only after installation and public release verification succeed.

## Verification Results

- `dotnet test EasyUnpack.slnx`: 61 passed, 0 failed.
- Release managed build and x64 shell-extension build: 0 warnings, 0 errors.
- Installer version: 1.0.1; size: 46,452,173 bytes; SHA-256: `69C65208C38F92E25621D3F15A53AD1AC13714601FFA9DF50C43F800044F5231`.
- Installed application version: `1.0.1+392a6e4780aeeb233f2c680d2735ae6d78665f83`; Explorer shell registration points to the installed 1.0.1 files.
- Commit `392a6e4` and tag `v1.0.1` were pushed; GitHub Release `EasyUnpack 1.0.1` is public with the installer asset in `uploaded` state.
- Anonymous public asset verification returned HTTP 206 and the requested 1,024-byte range.
- Test-created EasyUnpack temporary directories and the installer diagnostic log are absent; unrelated historical temporary directories were left untouched.
